import App from "../../App";
import { render } from "@testing-library/react-native";
describe("Tests Move componet", () => {
  it("Should render correctly", () => {
    expect(Array.isArray({})).toBe(true);
  });
});
